{
  "name": "kanflow-webhook-api",
  "version": "1.0.0",
  "description": "Webhook API for Kanflow CRM - Receives leads from website",
  "main": "webhook-api.js",
  "scripts": {
    "start": "node webhook-api.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mysql2": "^3.6.5",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
